generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

// template model
model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}
